version: "3.9"

services:
    backend:
        container_name: pibackend
        build: ./backend
        env_file: secrets.env
        volumes: 
            - ./backend/slackbridge/db.sqlite3:/app/db.sqlite3
        ports: 
            - "8000:8000"
